<UserControl x:Class="GoldShopWpf.Views.ModernReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Margin="8" Background="{DynamicResource AppBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ReportCaptureSurface">
                <Border Style="{StaticResource CardContainer}" Padding="28" Margin="0,0,0,18" BorderBrush="#E6EAF2">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="56" Height="56" CornerRadius="18" Background="#FFF4D6" Padding="8" Margin="0,0,16,0">
                                <Image Source="Resources/logo.png" Stretch="Uniform" />
                            </Border>
                            <StackPanel>
                                <TextBlock Text="{Binding CompanyName}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Text="{Binding ReportTitle}" FontSize="30" FontWeight="Bold" />
                            </StackPanel>
                        </StackPanel>

                        <Border Grid.Row="0" Grid.Column="1" Background="#F7F8FC" BorderBrush="#E3E8F0" BorderThickness="1" CornerRadius="18" Padding="18,12">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LblDate}" FontSize="12" Foreground="{StaticResource MutedTextBrush}" />
                                <TextBlock Text="{Binding CurrentDateDisplay}" FontSize="16" FontWeight="SemiBold" />
                            </StackPanel>
                        </Border>

                        <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                   Margin="0,18,0,0"
                                   Text="{DynamicResource LblModernReportSub}"
                                   Foreground="{StaticResource MutedTextBrush}" />

                        <WrapPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,24,0,0" Style="{StaticResource UnifiedActionBar}">
                            <Button Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"
                                    Command="{Binding ExportImageCommand}"
                                    CommandParameter="{Binding ElementName=ReportCaptureSurface}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ImageOutline" Margin="0,0,6,0" />
                                    <TextBlock Text="{DynamicResource BtnExportImage}" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource GhostButton}" Margin="0,0,10,0"
                                    Command="{Binding ExportPdfCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FilePdfBox" Margin="0,0,6,0" />
                                    <TextBlock Text="{DynamicResource BtnExportPdf}" />
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource GhostButton}"
                                    Command="{Binding ShareCommand}"
                                    CommandParameter="{Binding ElementName=ReportCaptureSurface}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ShareVariantOutline" Margin="0,0,6,0" />
                                    <TextBlock Text="{DynamicResource BtnShare}" />
                                </StackPanel>
                            </Button>
                        </WrapPanel>

                        <WrapPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,26,0,0">
                            <StackPanel Width="220" Margin="0,0,16,0">
                                <TextBlock Text="{DynamicResource LblFrom}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6" />
                                <DatePicker SelectedDate="{Binding FromDate}" Width="220" Margin="0" />
                            </StackPanel>
                            <StackPanel Width="220" Margin="0,0,16,0">
                                <TextBlock Text="{DynamicResource LblTo}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6" />
                                <DatePicker SelectedDate="{Binding ToDate}" Width="220" Margin="0" />
                            </StackPanel>
                            <Button Style="{StaticResource GhostButton}" Margin="0,24,0,0" MinWidth="120" Command="{Binding RefreshCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Refresh" Margin="0,0,6,0" />
                                    <TextBlock Text="{DynamicResource BtnRefresh}" />
                                </StackPanel>
                            </Button>
                        </WrapPanel>

                        <Grid Grid.Row="4" Grid.ColumnSpan="2" Margin="0,28,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <materialDesign:Card Grid.Column="0" Margin="0,0,12,0" Padding="20" Background="#F8FBFF">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource LblTotalWeightReport}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                    <TextBlock Text="{Binding TotalWeightDisplay}" FontSize="28" FontWeight="Bold" Margin="0,10,0,0" />
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:Card Grid.Column="1" Margin="12,0" Padding="20" Background="#FFF9F0">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource LblTotalValueReport}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                    <TextBlock Text="{Binding TotalValueDisplay}" FontSize="28" FontWeight="Bold" Margin="0,10,0,0" />
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:Card Grid.Column="2" Margin="12,0,0,0" Padding="20" Background="#F5FAF7">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource LblNetResultReport}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                    <TextBlock Text="{Binding NetResultDisplay}" FontSize="28" FontWeight="Bold" Margin="0,10,0,0" />
                                </StackPanel>
                            </materialDesign:Card>
                        </Grid>
                    </Grid>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.2*" />
                        <ColumnDefinition Width="1.1*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardContainer}" Margin="0,0,18,18" BorderBrush="#E7ECF2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="{DynamicResource LblModernGridTitle}" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock Text="{DynamicResource LblModernGridSub}" Foreground="{StaticResource MutedTextBrush}" />
                            </StackPanel>

                            <DataGrid Grid.Row="1"
                                      ItemsSource="{Binding ReportRows}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="Column">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="{DynamicResource LblDate}" Binding="{Binding Date, StringFormat={}{0:yyyy/MM/dd}}" Width="1.2*" ElementStyle="{StaticResource GridCellTextStyle}" />
                                    <DataGridTextColumn Header="{DynamicResource LblType}" Binding="{Binding Type}" Width="1.2*" ElementStyle="{StaticResource GridCellTextStyle}" />
                                    <DataGridTextColumn Header="{DynamicResource LblWeight}" Binding="{Binding Weight, StringFormat={}{0:0.####}}" Width="*" ElementStyle="{StaticResource GridCellTextStyle}" />
                                    <DataGridTextColumn Header="{DynamicResource LblItem}" Binding="{Binding Item}" Width="1.3*" ElementStyle="{StaticResource GridCellTextStyle}" />
                                    <DataGridTextColumn Header="{DynamicResource LblValue}" Binding="{Binding Value, StringFormat={}{0:0.##}}" Width="*" ElementStyle="{StaticResource GridCellTextStyle}" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="1">
                        <Border Style="{StaticResource CardContainer}" Margin="0,0,0,18" BorderBrush="#E7ECF2">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LblChartWeightDistribution}" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Text="{DynamicResource LblChartWeightDistributionSub}" Margin="0,4,0,16" Foreground="{StaticResource MutedTextBrush}" />
                                <ItemsControl ItemsSource="{Binding TopBalanceBars}" Height="240">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Width="58" Margin="5,0" VerticalAlignment="Bottom">
                                                <Border Background="#D4AF37" Height="{Binding Height}" CornerRadius="10,10,4,4" />
                                                <TextBlock Text="{Binding Label}" FontSize="10" TextAlignment="Center" Margin="0,8,0,0" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardContainer}" BorderBrush="#E7ECF2" Background="#FFFCF5">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource LblReportTotals}" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Text="{DynamicResource LblReportTotalsSub}" Margin="0,4,0,18" Foreground="{StaticResource MutedTextBrush}" />

                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource LblTotalGoldReport}" Foreground="{StaticResource MutedTextBrush}" />
                                    <TextBlock Grid.Column="1" Text="{Binding TotalGoldDisplay}" FontSize="18" FontWeight="SemiBold" />
                                </Grid>

                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource LblTotalManufacturingReport}" Foreground="{StaticResource MutedTextBrush}" />
                                    <TextBlock Grid.Column="1" Text="{Binding TotalManufacturingDisplay}" FontSize="18" FontWeight="SemiBold" />
                                </Grid>

                                <Border Background="#FFF4D7" BorderBrush="#EDD89A" BorderThickness="1" CornerRadius="16" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{DynamicResource LblNetTotalReport}" FontWeight="SemiBold" />
                                        <TextBlock Grid.Column="1" Text="{Binding NetTotalDisplay}" FontSize="20" FontWeight="Bold" />
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

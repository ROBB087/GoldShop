<UserControl x:Class="GoldShopWpf.Views.WeeklyReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardContainer}" Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource LblFrom}" VerticalAlignment="Center" Margin="0,0,8,0" />
                <DatePicker SelectedDate="{Binding FromDate}" Width="140" />
                <TextBlock Text="{DynamicResource LblTo}" VerticalAlignment="Center" Margin="16,0,8,0" />
                <DatePicker SelectedDate="{Binding ToDate}" Width="140" />
                <Button Content="{DynamicResource BtnRefresh}" Style="{StaticResource GhostButton}" Margin="16,0,8,0" Command="{Binding RefreshCommand}" ToolTip="{DynamicResource BtnRefresh}" />
                <Button Content="{DynamicResource BtnPrint}" Style="{StaticResource GhostButton}" Click="OnPrint" ToolTip="{DynamicResource BtnPrint}" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardContainer}" Grid.Column="0">
                <DataGrid ItemsSource="{Binding Reports}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource LblSupplier}" Binding="{Binding SupplierName}" Width="*" />
                        <DataGridTextColumn Header="{DynamicResource LblGoldGiven}" Binding="{Binding TotalGold, StringFormat=C}" Width="140" />
                        <DataGridTextColumn Header="{DynamicResource LblTotalPayments}" Binding="{Binding TotalPayments, StringFormat=C}" Width="140" />
                        <DataGridTextColumn Header="{DynamicResource LblCurrentBalance}" Binding="{Binding CurrentBalance, StringFormat=C}" Width="160" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Style="{StaticResource CardContainer}" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="{DynamicResource LblTopBalances}" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <ItemsControl ItemsSource="{Binding TopBalanceBars}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="50" Margin="4,0" VerticalAlignment="Bottom">
                                    <Border Background="{StaticResource GoldBrush}" Height="{Binding Height}" CornerRadius="4" />
                                    <TextBlock Text="{Binding Label}" TextWrapping="Wrap" FontSize="10" TextAlignment="Center" Margin="0,6,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

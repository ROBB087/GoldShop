<UserControl x:Class="GoldShopWpf.Views.WeeklyReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,18"
                Padding="28"
                CornerRadius="28"
                BorderBrush="#E4E9F6"
                BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#F8FBFF" Offset="0" />
                    <GradientStop Color="#EDF4FF" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{DynamicResource NavWeeklyReport}" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="{DynamicResource LblWeeklyReportSub}"
                               FontSize="15"
                               Foreground="#53647F"
                               Margin="0,0,0,18" />
                    <WrapPanel>
                        <StackPanel MinWidth="180" Margin="0,0,16,8">
                            <TextBlock Text="{DynamicResource LblFrom}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6" />
                            <DatePicker SelectedDate="{Binding FromDate}" Margin="0" />
                        </StackPanel>
                        <StackPanel MinWidth="180" Margin="0,0,16,8">
                            <TextBlock Text="{DynamicResource LblTo}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6" />
                            <DatePicker SelectedDate="{Binding ToDate}" Margin="0" />
                        </StackPanel>
                    </WrapPanel>
                    <Border Margin="0,10,0,0"
                            Padding="14,10"
                            Background="#FFF7E2"
                            BorderBrush="#F0DFA7"
                            BorderThickness="1"
                            CornerRadius="16">
                        <StackPanel>
                            <TextBlock Text="{Binding WeekRangeDisplay}" FontWeight="SemiBold" Foreground="#8A6C00" />
                            <TextBlock Text="{Binding WeekRuleNote}" FontSize="12" Foreground="#7A5D00" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <WrapPanel Grid.Column="1" VerticalAlignment="Top">
                    <Button Style="{StaticResource GhostButton}" Margin="0,0,10,0" Command="{Binding RefreshCommand}" ToolTip="{DynamicResource BtnRefresh}" MinWidth="126">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnRefresh}" />
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource GhostButton}" Click="OnPrint" ToolTip="{DynamicResource BtnPrint}" MinWidth="118">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnPrint}" />
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardContainer}" Grid.Row="0" Margin="0,0,0,18" BorderBrush="#E7ECF2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{DynamicResource LblWeeklyPerformanceTable}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Reports}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource LblTrader}" Binding="{Binding SupplierName}" Width="2*" MinWidth="140" ElementStyle="{StaticResource GridCellTextStyle}" />
                            <DataGridTextColumn Header="{DynamicResource LblTotalGold21}" Binding="{Binding TotalGold21, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Width="*" MinWidth="110" ElementStyle="{StaticResource GridCellTextStyle}" />
                            <DataGridTextColumn Header="{DynamicResource LblTotalManufacturing}" Binding="{Binding TotalManufacturing, Converter={StaticResource MoneyFormatConverter}}" Width="*" MinWidth="120" ElementStyle="{StaticResource GridCellTextStyle}" />
                            <DataGridTextColumn Header="{DynamicResource LblTotalImprovement}" Binding="{Binding TotalImprovement, Converter={StaticResource MoneyFormatConverter}}" Width="*" MinWidth="120" ElementStyle="{StaticResource GridCellTextStyle}" />
                            <DataGridTextColumn Header="{DynamicResource LblFinalManufacturing}" Binding="{Binding FinalManufacturing, Converter={StaticResource MoneyFormatConverter}}" Width="*" MinWidth="120" ElementStyle="{StaticResource GridCellTextStyle}" />
                            <DataGridTextColumn Header="{DynamicResource LblFinalImprovement}" Binding="{Binding FinalImprovement, Converter={StaticResource MoneyFormatConverter}}" Width="*" MinWidth="120" ElementStyle="{StaticResource GridCellTextStyle}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardContainer}" Grid.Row="1" Margin="0" BorderBrush="#E7ECF2">
                <StackPanel>
                    <TextBlock Text="{DynamicResource LblTopGold}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBlock Text="{DynamicResource LblTopGoldSub}"
                               Foreground="{StaticResource MutedTextBrush}"
                               Margin="0,0,0,18" />
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding TopBalanceBars}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="56" Margin="5,0" VerticalAlignment="Bottom">
                                    <Border Background="#E1B325" Height="{Binding Height}" CornerRadius="6,6,4,4" />
                                    <TextBlock Text="{Binding Label}" TextWrapping="Wrap" FontSize="10" TextAlignment="Center" Margin="0,8,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

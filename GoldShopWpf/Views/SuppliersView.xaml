<UserControl x:Class="GoldShopWpf.Views.SuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardContainer}" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox Width="280" Height="32" VerticalContentAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource LblSearch}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <Button Command="{Binding AddCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnAddTrader}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnAddTrader}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnEdit}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnEdit}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnDelete}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnDelete}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenDetailsCommand}" Style="{StaticResource GhostButton}" ToolTip="{DynamicResource BtnViewDetails}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="OpenInNew" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnViewDetails}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="1">
            <DataGrid ItemsSource="{Binding FilteredSuppliers}" SelectedItem="{Binding SelectedSupplier}" AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True" CanUserSortColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource LblTrader}" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="{DynamicResource LblPhone}" Binding="{Binding Phone}" Width="180" />
                    <DataGridTextColumn Header="{DynamicResource LblTotalGold21}" Binding="{Binding TotalGold21, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Width="180" />
                    <DataGridTextColumn Header="{DynamicResource LblNetGold21}" Binding="{Binding NetGold21, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Width="180" />
                    <DataGridTextColumn Header="{DynamicResource LblLastUpdated}" Binding="{Binding LastActivityLabel}" Width="170" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>


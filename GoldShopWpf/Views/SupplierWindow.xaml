<Window x:Class="GoldShopWpf.Views.SupplierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Supplier" Height="340" Width="480" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{DynamicResource LblName}" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
        <TextBox x:Name="NameText" Grid.Row="0" Grid.Column="1" Height="32" />

        <TextBlock Text="Phone" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0" />
        <TextBox x:Name="PhoneText" Grid.Row="1" Grid.Column="1" Height="32" Margin="0,10,0,0" />

        <TextBlock Text="{DynamicResource LblNotes}" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" Margin="0,10,0,0" />
        <TextBox x:Name="NotesText" Grid.Row="2" Grid.Column="1" Margin="0,10,0,0" AcceptsReturn="True" TextWrapping="Wrap" />

        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="{DynamicResource BtnApply}" Width="120" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Click="OnSave" ToolTip="{DynamicResource BtnApply}" />
            <Button Content="{DynamicResource BtnClear}" Width="100" Style="{StaticResource GhostButton}" Click="OnCancel" ToolTip="{DynamicResource BtnClear}" />
        </StackPanel>
    </Grid>
</Window>

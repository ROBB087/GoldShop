<UserControl x:Class="GoldShopWpf.Views.NotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,18"
                Padding="28"
                CornerRadius="28"
                BorderBrush="#E5E9F2"
                BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#F8FBFF" Offset="0" />
                    <GradientStop Color="#EDF4FF" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="{DynamicResource NavNotes}" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="{DynamicResource LblNotesPageSub}"
                               FontSize="15"
                               Foreground="#53647F"
                               Margin="0,0,0,18" />
                    <Border Background="#FFFFFF"
                            BorderBrush="#DCE6F5"
                            BorderThickness="1"
                            CornerRadius="18"
                            Padding="14,10"
                            Width="360">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Width="290"
                                     Margin="0"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="{DynamicResource LblSearch}"
                                     materialDesign:HintAssist.Hint="{DynamicResource LblSearch}" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="{DynamicResource BtnRefresh}"
                        Style="{StaticResource GhostButton}"
                        Command="{Binding RefreshCommand}"
                        ToolTip="{DynamicResource BtnRefresh}" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardContainer}" BorderBrush="#E7ECF2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{DynamicResource LblNotesRegister}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Notes}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource LblClientName}" Binding="{Binding ClientName}" Width="220" MinWidth="220" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblNotes}" Binding="{Binding Notes}" Width="*" MinWidth="320" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblDate}" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="160" MinWidth="160" ElementStyle="{StaticResource GridCellTextStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>

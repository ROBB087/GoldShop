<UserControl x:Class="GoldShopWpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="16" ShadowDepth="2" Opacity="0.12" />

        <Style x:Key="MetricCard" TargetType="materialDesign:Card">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Effect" Value="{StaticResource CardShadow}" />
        </Style>

        <Style x:Key="MetricTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="MetricValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>

        <Style x:Key="IconBubble" TargetType="Border">
            <Setter Property="Width" Value="38" />
            <Setter Property="Height" Value="38" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Border Style="{StaticResource CardContainer}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblOverview}" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource LblOverviewSub}" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Column="1">
                        <Button Command="{Binding AddSupplierCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" ToolTip="Add a new trader">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,6,0" />
                                <TextBlock Text="{DynamicResource BtnAddTrader}" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding AddTransactionCommand}" Style="{StaticResource GhostButton}" ToolTip="Record trader transaction">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cash" Width="18" Height="18" Margin="0,0,6,0" />
                                <TextBlock Text="{DynamicResource BtnAddTransaction}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <UniformGrid Columns="4" Margin="0,0,0,16">
                <materialDesign:Card Style="{StaticResource MetricCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalGold21}" Style="{StaticResource MetricTitle}" />
                            <TextBlock Text="{Binding TotalGold21, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Style="{StaticResource MetricValue}" />
                        </StackPanel>
                        <Border Style="{StaticResource IconBubble}" Background="#FFF5E6" Grid.Column="1">
                            <materialDesign:PackIcon Kind="CashMultiple" Width="20" Height="20" Foreground="#C9A300" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Style="{StaticResource MetricCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTraders}" Style="{StaticResource MetricTitle}" />
                            <TextBlock Text="{Binding SupplierCount}" Style="{StaticResource MetricValue}" />
                        </StackPanel>
                        <Border Style="{StaticResource IconBubble}" Background="#EEF2FF" Grid.Column="1">
                            <materialDesign:PackIcon Kind="AccountGroup" Width="20" Height="20" Foreground="#3F51B5" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Style="{StaticResource MetricCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalManufacturing}" Style="{StaticResource MetricTitle}" />
                            <TextBlock Text="{Binding TotalManufacturingThisWeek, Converter={StaticResource MoneyFormatConverter}}" Style="{StaticResource MetricValue}" />
                        </StackPanel>
                        <Border Style="{StaticResource IconBubble}" Background="#FFF8E1" Grid.Column="1">
                            <materialDesign:PackIcon Kind="DiamondStone" Width="20" Height="20" Foreground="#C9A300" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Style="{StaticResource MetricCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalImprovement}" Style="{StaticResource MetricTitle}" />
                            <TextBlock Text="{Binding TotalImprovementThisWeek, Converter={StaticResource MoneyFormatConverter}}" Style="{StaticResource MetricValue}" />
                        </StackPanel>
                        <Border Style="{StaticResource IconBubble}" Background="#E8F5E9" Grid.Column="1">
                            <materialDesign:PackIcon Kind="BankTransfer" Width="20" Height="20" Foreground="#2E7D32" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                    </Grid>
                </materialDesign:Card>
            </UniformGrid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource CardContainer}" Grid.Column="0">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblTopGold}" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding SupplierBalanceBars}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Width="50" Margin="4,0" VerticalAlignment="Bottom">
                                        <Border Background="{StaticResource GoldBrush}" Height="{Binding Height}" CornerRadius="4" />
                                        <TextBlock Text="{Binding Label}" TextWrapping="Wrap" FontSize="10" TextAlignment="Center" Margin="0,6,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardContainer}" Grid.Column="1">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblGoldVsCosts}" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="200" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Viewbox Stretch="Uniform" Height="200">
                                <Canvas Height="200" Width="520">
                                    <Polyline Points="{Binding GoldLinePoints}" Stroke="{StaticResource GoldBrush}" StrokeThickness="2" />
                                    <Polyline Points="{Binding PaymentLinePoints}" Stroke="{StaticResource AccentGreenBrush}" StrokeThickness="2" />
                                </Canvas>
                            </Viewbox>
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding GoldPaymentLabels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="7" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" FontSize="10" TextAlignment="Center" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>


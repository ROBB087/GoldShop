<UserControl x:Class="GoldShopWpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Border Style="{StaticResource CardContainer}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblOverview}" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource LblOverviewSub}" Foreground="{StaticResource MutedTextBrush}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Column="1">
                        <Button Command="{Binding AddSupplierCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" ToolTip="Add a new supplier">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,6,0" />
                                <TextBlock Text="{DynamicResource BtnAddSupplier}" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding AddTransactionCommand}" Style="{StaticResource GhostButton}" ToolTip="Record gold or payment">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cash" Width="18" Height="18" Margin="0,0,6,0" />
                                <TextBlock Text="{DynamicResource BtnAddTransaction}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <UniformGrid Columns="4" Margin="0,0,0,16">
                <Border Style="{StaticResource CardContainer}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblOutstanding}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding TotalOutstanding, StringFormat=C}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource GoldValueBrush}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardContainer}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblSuppliers}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding SupplierCount}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource GoldValueBrush}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardContainer}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblGoldGiven}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding TotalGoldIssued, StringFormat=C}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource GoldValueBrush}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardContainer}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblPaymentsWeek}" Foreground="{StaticResource MutedTextBrush}" />
                        <TextBlock Text="{Binding TotalPaymentsThisWeek, StringFormat=C}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource GoldValueBrush}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource CardContainer}" Grid.Column="0">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblTopBalances}" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding SupplierBalanceBars}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Width="50" Margin="4,0" VerticalAlignment="Bottom">
                                        <Border Background="{StaticResource GoldBrush}" Height="{Binding Height}" CornerRadius="4" />
                                        <TextBlock Text="{Binding Label}" TextWrapping="Wrap" FontSize="10" TextAlignment="Center" Margin="0,6,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardContainer}" Grid.Column="1">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource LblGoldVsPayment}" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="200" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Viewbox Stretch="Uniform" Height="200">
                                <Canvas Height="200" Width="520">
                                    <Polyline Points="{Binding GoldLinePoints}" Stroke="{StaticResource GoldBrush}" StrokeThickness="2" />
                                    <Polyline Points="{Binding PaymentLinePoints}" Stroke="{StaticResource AccentGreenBrush}" StrokeThickness="2" />
                                </Canvas>
                            </Viewbox>
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding GoldPaymentLabels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="7" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" FontSize="10" TextAlignment="Center" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>

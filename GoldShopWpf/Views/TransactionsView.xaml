<UserControl x:Class="GoldShopWpf.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardContainer}" Grid.Row="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <WrapPanel Grid.Row="0" ItemHeight="36" ItemWidth="220">
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,8">
                        <TextBlock Text="{DynamicResource LblSupplier}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox Width="180" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,8">
                        <TextBlock Text="{DynamicResource LblFrom}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <DatePicker SelectedDate="{Binding FromDate}" Width="140" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,8">
                        <TextBlock Text="{DynamicResource LblTo}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <DatePicker SelectedDate="{Binding ToDate}" Width="140" />
                    </StackPanel>
                </WrapPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                        <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Width="220" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource LblSearch}" />
                    </StackPanel>
                    <Button Content="{DynamicResource BtnRefresh}" Style="{StaticResource GhostButton}" Width="120" Height="36" Command="{Binding RefreshCommand}" ToolTip="{DynamicResource BtnRefresh}" />
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Command="{Binding AddCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,12,0" Width="150" Height="38" ToolTip="{DynamicResource BtnAddTransaction}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cash" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnAddTransaction}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditCommand}" Style="{StaticResource GhostButton}" Margin="0,0,12,0" Width="110" Height="38" ToolTip="{DynamicResource BtnEdit}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnEdit}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteCommand}" Style="{StaticResource GhostButton}" Width="110" Height="38" ToolTip="{DynamicResource BtnDelete}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnDelete}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="1">
            <DataGrid ItemsSource="{Binding FilteredTransactions}" SelectedItem="{Binding SelectedTransaction}" AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True" CanUserSortColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource LblDate}" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="140" />
                    <DataGridTextColumn Header="{DynamicResource LblSupplier}" Binding="{Binding SupplierName}" Width="*" />
                    <DataGridTextColumn Header="{DynamicResource LblType}" Binding="{Binding Type, Converter={StaticResource TransactionTypeToLabelConverter}}" Width="180">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{Binding Type, Converter={StaticResource TransactionTypeToBrushConverter}}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{DynamicResource LblDescription}" Binding="{Binding Details}" Width="*" />
                    <DataGridTextColumn Header="{DynamicResource LblAmount}" Binding="{Binding Amount, StringFormat=C}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="GoldShopWpf.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardContainer}" Grid.Row="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource LblTrader}" VerticalAlignment="Center" Margin="5" />
                <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" Margin="5" />

                <TextBlock Grid.Row="0" Grid.Column="3" Text="{DynamicResource LblFrom}" VerticalAlignment="Center" Margin="5" />
                <DatePicker Grid.Row="0" Grid.Column="4" SelectedDate="{Binding FromDate}" Width="140" Margin="5" materialDesign:HintAssist.Hint="من" />

                <TextBlock Grid.Row="0" Grid.Column="6" Text="{DynamicResource LblTo}" VerticalAlignment="Center" Margin="5" />
                <DatePicker Grid.Row="0" Grid.Column="7" SelectedDate="{Binding ToDate}" Width="140" Margin="5" materialDesign:HintAssist.Hint="إلى" />

                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="8" Orientation="Horizontal" Margin="5,6,5,0">
                    <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox Width="240" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{DynamicResource LblSearch}" materialDesign:HintAssist.Hint="{DynamicResource LblSearch}" />
                    <Button Content="{DynamicResource BtnRefresh}" Style="{StaticResource GhostButton}" Margin="12,0,0,0" Command="{Binding RefreshCommand}" ToolTip="{DynamicResource BtnRefresh}" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Command="{Binding AddCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,12,0" ToolTip="{DynamicResource BtnAddTransaction}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cash" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnAddTransaction}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditCommand}" Style="{StaticResource GhostButton}" Margin="0,0,12,0" ToolTip="{DynamicResource BtnEdit}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnEdit}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteCommand}" Style="{StaticResource GhostButton}" ToolTip="{DynamicResource BtnDelete}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="0,0,6,0" Foreground="#D32F2F" />
                            <TextBlock Text="{DynamicResource BtnDelete}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="1">
            <DataGrid ItemsSource="{Binding FilteredTransactions}" SelectedItem="{Binding SelectedTransaction}" AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True" CanUserSortColumns="True" SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource LblDate}" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="130" MinWidth="130" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblTrader}" Binding="{Binding SupplierName}" Width="2*" MinWidth="150" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblType}" Binding="{Binding Type, Converter={StaticResource TransactionTypeToLabelConverter}}" Width="*" MinWidth="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource GridCellTextStyle}">
                                <Setter Property="Foreground" Value="{Binding Type, Converter={StaticResource TransactionTypeToBrushConverter}}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{DynamicResource LblWeight}" Binding="{Binding OriginalWeight, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Width="*" MinWidth="110" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblKarat}" Binding="{Binding OriginalKarat}" Width="*" MinWidth="90" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblEquivalent21}" Binding="{Binding Equivalent21, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Width="*" MinWidth="130" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblTotalManufacturing}" Binding="{Binding TotalManufacturing, Converter={StaticResource MoneyFormatConverter}}" Width="*" MinWidth="160" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblTotalImprovement}" Binding="{Binding TotalImprovement, Converter={StaticResource MoneyFormatConverter}}" Width="*" MinWidth="160" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblTraceability}" Binding="{Binding Traceability}" Width="2*" MinWidth="220" ElementStyle="{StaticResource GridCellTextStyle}" />
                    <DataGridTextColumn Header="{DynamicResource LblUpdatedAt}" Binding="{Binding UpdatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="170" MinWidth="170" ElementStyle="{StaticResource GridCellTextStyle}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>



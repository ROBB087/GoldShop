<UserControl x:Class="GoldShopWpf.Views.StatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardContainer}" Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource LblSupplier}" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox Width="220" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" />
                <TextBlock Text="{DynamicResource LblFrom}" VerticalAlignment="Center" Margin="16,0,8,0" />
                <DatePicker SelectedDate="{Binding FromDate}" Width="140" />
                <TextBlock Text="{DynamicResource LblTo}" VerticalAlignment="Center" Margin="16,0,8,0" />
                <DatePicker SelectedDate="{Binding ToDate}" Width="140" />
                <Button Content="{DynamicResource BtnApply}" Style="{StaticResource PrimaryButton}" Margin="16,0,0,0" Command="{Binding GenerateCommand}" ToolTip="{DynamicResource BtnApply}" />
                <Button Content="{DynamicResource BtnPrint}" Style="{StaticResource GhostButton}" Margin="8,0,0,0" Command="{Binding PrintCommand}" ToolTip="{DynamicResource BtnPrint}" />
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="1">
            <TextBox Text="{Binding StatementText}" IsReadOnly="True" TextWrapping="NoWrap" FontFamily="{DynamicResource AppFontFamily}" FontSize="12"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
        </Border>
    </Grid>
</UserControl>

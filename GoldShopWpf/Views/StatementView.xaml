<UserControl x:Class="GoldShopWpf.Views.StatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <Border Margin="0,0,0,18"
                    Padding="28"
                    CornerRadius="28"
                    BorderBrush="#E8E3DA"
                    BorderThickness="1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFFDF8" Offset="0" />
                        <GradientStop Color="#F8F1E3" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{DynamicResource NavStatement}" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <TextBlock Text="{DynamicResource LblStatementSub}"
                                   FontSize="15"
                                   Foreground="#6B5B2A"
                                   Margin="0,0,0,18" />
                        <WrapPanel>
                            <StackPanel Width="240" Margin="0,0,16,0">
                                <TextBlock Text="{DynamicResource LblSupplier}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6" />
                                <ComboBox Width="240" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" Margin="0" />
                            </StackPanel>
                            <StackPanel Width="220" Margin="0,0,16,0">
                                <TextBlock Text="{DynamicResource LblFrom}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6" />
                                <DatePicker SelectedDate="{Binding FromDate}" Width="220" Margin="0" />
                            </StackPanel>
                            <StackPanel Width="220">
                                <TextBlock Text="{DynamicResource LblTo}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6" />
                                <DatePicker SelectedDate="{Binding ToDate}" Width="220" Margin="0" />
                            </StackPanel>
                        </WrapPanel>
                    </StackPanel>

                    <WrapPanel Grid.Column="1"
                               VerticalAlignment="Top">
                        <Button Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Command="{Binding GenerateCommand}" ToolTip="{DynamicResource BtnApply}" MinWidth="136">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileDocumentOutline" Width="18" Height="18" Margin="0,0,6,0" />
                                <TextBlock Text="{DynamicResource BtnApply}" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource GhostButton}" Command="{Binding PrintCommand}" ToolTip="{DynamicResource BtnPrint}" MinWidth="118">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Printer" Width="18" Height="18" Margin="0,0,6,0" />
                                <TextBlock Text="{DynamicResource BtnPrint}" />
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardContainer}"
                    BorderBrush="#E7ECF2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="{DynamicResource LblStatementPreview}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />
                    <TextBox Grid.Row="1"
                             Text="{Binding StatementText}"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             FontFamily="{DynamicResource AppFontFamily}"
                             FontSize="12"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto" />
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

<UserControl x:Class="GoldShopWpf.Views.SupplierDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardContainer}" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{DynamicResource NavSupplierDetails}" FontSize="18" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource LblSupplier}" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,8,0" />
                        <ComboBox Width="220" ItemsSource="{Binding SupplierOptions}" SelectedItem="{Binding SelectedSupplierOption}" DisplayMemberPath="Name" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <Button Command="{Binding AddGoldCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnAddGold}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnAddGold}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding AddPaymentCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnAddPayment}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cash" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnAddPayment}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditTransactionCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnEdit}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnEdit}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteTransactionCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnDelete}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnDelete}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding PrintStatementCommand}" Style="{StaticResource GhostButton}" ToolTip="{DynamicResource BtnPrint}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource NavStatement}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Text="{DynamicResource LblFrom}" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding FromDate}" Width="140" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="0,0,16,0">
                    <TextBlock Text="{DynamicResource LblTo}" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <DatePicker SelectedDate="{Binding ToDate}" Width="140" />
                </StackPanel>
                <Button Grid.Column="2" Content="{DynamicResource BtnApply}" Command="{Binding ApplyFilterCommand}" Style="{StaticResource GhostButton}" Margin="0,0,8,0" ToolTip="{DynamicResource BtnApply}" />
                <Button Grid.Column="3" Content="{DynamicResource BtnClear}" Command="{Binding ClearFilterCommand}" Style="{StaticResource GhostButton}" ToolTip="{DynamicResource BtnClear}" />

                <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Background="#FFF3E0" CornerRadius="8" Padding="10" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblCurrentBalance}" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Text="{Binding CurrentBalance, StringFormat=C}" FontWeight="SemiBold" Foreground="{DynamicResource GoldValueBrush}" />
                        </StackPanel>
                    </Border>
                    <Border Background="#FFF8E1" CornerRadius="8" Padding="10" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalGold}" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Text="{Binding TotalDebit, StringFormat=C}" FontWeight="SemiBold" Foreground="{DynamicResource GoldValueBrush}" />
                        </StackPanel>
                    </Border>
                    <Border Background="#E8F5E9" CornerRadius="8" Padding="10">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalPayments}" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBlock Text="{Binding TotalCredit, StringFormat=C}" FontWeight="SemiBold" Foreground="{DynamicResource GoldValueBrush}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="2">
            <DataGrid ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction}" AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource LblSupplier}" Binding="{Binding SupplierName}" Width="160" />
                    <DataGridTextColumn Header="{DynamicResource LblDate}" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="140" />
                    <DataGridTextColumn Header="{DynamicResource LblType}" Binding="{Binding Type, Converter={StaticResource TransactionTypeToLabelConverter}}" Width="160">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{Binding Type, Converter={StaticResource TransactionTypeToBrushConverter}}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="{DynamicResource LblDescription}" Binding="{Binding Description}" Width="*" />
                    <DataGridTextColumn Header="{DynamicResource LblAmount}" Binding="{Binding Amount, StringFormat=C}" Width="140" />
                    <DataGridTextColumn Header="{DynamicResource LblBalance}" Binding="{Binding Balance, StringFormat=C}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="GoldShopWpf.Views.SupplierDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardContainer}" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{DynamicResource NavTraderDetails}" FontSize="20" FontWeight="SemiBold" Margin="8" />
                    <StackPanel Orientation="Horizontal" Margin="8">
                        <TextBlock Text="{DynamicResource LblTrader}" Foreground="{StaticResource MutedTextBrush}" Margin="8" />
                        <ComboBox Width="260" Margin="8" ItemsSource="{Binding SupplierOptions}" SelectedItem="{Binding SelectedSupplierOption}" DisplayMemberPath="Name" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="8" HorizontalAlignment="Right">
                    <Button Command="{Binding AddTransactionCommand}" Style="{StaticResource PrimaryButton}" Margin="8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnAddTransaction}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding AddDiscountCommand}" Style="{StaticResource GhostButton}" Margin="8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Percent" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnAddDiscount}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditTransactionCommand}" Style="{StaticResource GhostButton}" Margin="8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource BtnEdit}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteTransactionCommand}" Style="{StaticResource GhostButton}" Margin="8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="0,0,6,0" Foreground="#D32F2F" />
                            <TextBlock Text="{DynamicResource BtnDelete}" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding PrintStatementCommand}" Style="{StaticResource GhostButton}" Margin="8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="{DynamicResource NavStatement}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{DynamicResource LblFrom}" VerticalAlignment="Center" Margin="5" />
                    <DatePicker Grid.Column="1" SelectedDate="{Binding FromDate}" Width="140" Margin="5" />
                    <TextBlock Grid.Column="2" Text="{DynamicResource LblTo}" VerticalAlignment="Center" Margin="5" />
                    <DatePicker Grid.Column="3" SelectedDate="{Binding ToDate}" Width="140" Margin="5" />
                    <Button Grid.Column="4" Width="100" Content="{DynamicResource BtnApply}" Command="{Binding ApplyFilterCommand}" Style="{StaticResource GhostButton}" Margin="5" />
                    <Button Grid.Column="5" Width="100" Content="{DynamicResource BtnClear}" Command="{Binding ClearFilterCommand}" Style="{StaticResource GhostButton}" Margin="5" />
                </Grid>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="2">
            <StackPanel Margin="8">
                <TextBlock Text="Summary" FontSize="16" FontWeight="SemiBold" Margin="8" />
                <WrapPanel Margin="8">
                    <materialDesign:Card Width="220" Margin="8" Padding="18" Background="#FFFFFF">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalGold21}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding TotalGold21, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" />
                        </StackPanel>
                    </materialDesign:Card>
                    <materialDesign:Card Width="220" Margin="8" Padding="18" Background="#FFFFFF">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalManufacturing}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding TotalManufacturing, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" />
                        </StackPanel>
                    </materialDesign:Card>
                    <materialDesign:Card Width="220" Margin="8" Padding="18" Background="#FFFFFF">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblTotalImprovement}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding TotalImprovement, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" />
                        </StackPanel>
                    </materialDesign:Card>
                    <materialDesign:Card Width="220" Margin="8" Padding="18" Background="#FFFFFF">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblManufacturingDiscounts}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding ManufacturingDiscounts, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" FontSize="24" FontWeight="Bold" Foreground="#B71C1C" />
                        </StackPanel>
                    </materialDesign:Card>
                    <materialDesign:Card Width="220" Margin="8" Padding="18" Background="#FFFFFF">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource LblImprovementDiscounts}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding ImprovementDiscounts, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" FontSize="24" FontWeight="Bold" Foreground="#B71C1C" />
                        </StackPanel>
                    </materialDesign:Card>
                    <materialDesign:Card Width="220" Margin="8" Padding="18" Background="#FFFFFF">
                        <StackPanel>
                            <TextBlock Text="Net Manufacturing" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding NetManufacturing, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" FontSize="24" FontWeight="Bold" Foreground="#1A1A1A" />
                        </StackPanel>
                    </materialDesign:Card>
                    <materialDesign:Card Width="220" Margin="8" Padding="18" Background="#FFFFFF">
                        <StackPanel>
                            <TextBlock Text="Net Improvement" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding NetImprovement, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" FontSize="24" FontWeight="Bold" Foreground="#1A1A1A" />
                        </StackPanel>
                    </materialDesign:Card>
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="3">
            <StackPanel>
                <TextBlock Text="Transactions" FontSize="16" FontWeight="SemiBold" Margin="8" />
                <DataGrid ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction}" AutoGenerateColumns="False"
                          CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow" MinHeight="280" Margin="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource LblDate}" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="130" MinWidth="130" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblType}" Binding="{Binding Type, Converter={StaticResource TransactionTypeToLabelConverter}}" Width="110" MinWidth="110" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblWeight}" Binding="{Binding OriginalWeight, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Width="120" MinWidth="120" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblKarat}" Binding="{Binding OriginalKarat}" Width="90" MinWidth="90" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblEquivalent21}" Binding="{Binding Equivalent21, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='g'}" Width="130" MinWidth="130" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblTotalManufacturing}" Binding="{Binding TotalManufacturing, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" Width="160" MinWidth="160" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblTotalImprovement}" Binding="{Binding TotalImprovement, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" Width="160" MinWidth="160" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblNotes}" Binding="{Binding Notes}" Width="2*" MinWidth="200" ElementStyle="{StaticResource GridCellTextStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardContainer}" Grid.Row="4">
            <StackPanel>
                <TextBlock Text="Discounts" FontSize="16" FontWeight="SemiBold" Margin="8" />
                <DataGrid ItemsSource="{Binding Discounts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="190" Margin="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource LblDate}" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd}}" Width="130" MinWidth="130" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblType}" Binding="{Binding Type}" Width="180" MinWidth="160" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblAmount}" Binding="{Binding Amount, Converter={StaticResource MoneyFormatConverter}, ConverterParameter='EGP'}" Width="160" MinWidth="160" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn Header="{DynamicResource LblNotes}" Binding="{Binding Notes}" Width="*" MinWidth="220" ElementStyle="{StaticResource GridCellTextStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
